import { NoShellComponent } from './no-shell.component';

describe('NoShellComponent', () => {
  let component: NoShellComponent;

  beforeEach(() => {
    component = new NoShellComponent();
  });

  it('should create', () => {
    // Assert
    expect(component).toBeTruthy();
  });
});