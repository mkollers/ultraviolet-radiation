import { ShellComponent } from './shell.component';

describe('ShellComponent', () => {
  it('should create', () => {
    // Act
    const component = new ShellComponent();

    // Assert
    expect(component).toBeTruthy();
  });
});
